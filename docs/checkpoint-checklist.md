# 数据交付检查表

- [ ] 区域文件夹只包含对应区域的 `map.png`、`area.json` 和必要说明。
- [ ] 标注期间没有更换、缩放或裁剪 `map.png`。
- [ ] 所有地点都绑定有效 `routeNodeId`。
- [ ] 所有节点只有 `id,type,x,y`。
- [ ] 所有边都引用存在的节点，且没有重复连边。
- [ ] 道路弯折处通过中间节点表达。
- [ ] 重要地点之间实际可达，路线没有穿过建筑或不可通行区域。
- [ ] 已运行 `npm run validate:data`，结果为 `0 fatal`。
- [ ] 已运行 `npm run check`，编码、JavaScript 语法和地图数据检查全部通过。
- [ ] 后端启动后 `/api/v1/health`、区域列表、区域数据和地图图片接口可访问。
- [ ] 已运行后端 API 自动测试，且没有将地图数据复制到后端目录。
- [ ] 已将确认无误的边从 `draft` 改为 `reviewed`。
