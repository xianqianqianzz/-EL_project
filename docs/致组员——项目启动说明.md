# 致组员——项目启动说明

## 一、我们要做什么

**南京大学智能校园地图网站**。一句话说：能搜地点、能在地图上看到路线、点进建筑能看到每层楼的走廊和房间、告诉你从A到B怎么走（在外面走哪条路、进了楼走哪个楼梯上几楼）。

面向的是南京大学仙林校区。初期检查（半个月后）交三样东西：**技术文档 + PPT + 核心代码**。最终（一个月后）交完整项目。

---

## 二、我们怎么做的决策

下面是我们讨论后达成的一致意见，知道"为什么"比知道"是什么"重要：

### 技术方面

| 决定 | 原因 |
|------|------|
| 初期只用 HTML/CSS/JS，不加框架 | 先做出能跑的东西应付初期检查，框架学习成本太高 |
| 但地图用了 Leaflet.js 库 | 纯手写地图两周根本做不出来，Leaflet 小而精，不绑定框架 |
| 室内地图用 Canvas 画 | 室内不像室外有现成地图瓦片，得自己画走廊房间，Canvas 最灵活 |
| 路径规划用 A* 算法自己写 | 算法本身不难，自写可控，后期优化也方便 |
| 数据全用 JSON 文件 | 不用数据库，不用后端，静态文件就能跑 |
| 初期检查后再考虑重构 | 先让东西能跑，后面有时间再引入 React/Vue 等现代框架 |

### 协作方面

| 决定 | 原因 |
|------|------|
| GitHub 协作 | 免费，业界标准，老师也认可 |
| 每人一个开发分支 | 互不干扰，出问题不影响别人 |
| PR 合并到 main | 有人把关，避免乱合并 |
| 本地演示（不部署服务器） | 初期检查不需要线上部署，省事 |

---

## 三、项目架构（一图看懂）

```
用户打开网页
      │
      ▼
┌─────────────────┐
│   搜索起点/终点   │  ← C 负责的界面
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   图里找路径     │  ← B 负责的 A* 算法
│  (室外路 + 室内走廊)│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   画在地图上     │  ← A 负责的地图渲染
│  (室外Leaflet    │
│   室内Canvas)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   显示路线详情   │  ← C 负责的信息面板
└─────────────────┘
```

数据（地图、建筑、路网）由 D 负责准备，ABC 三人从 `js/data/loader.js` 统一读取。

---

## 四、你的任务

### 🅰️ 地图渲染（map/）

**你负责"地图怎么显示"**

具体要做的：
- 用 Leaflet 把 NJU 校园底图显示出来，能缩放拖动
- 把 `buildings.json` 里的建筑画成多边形，点击弹信息
- 把 `poi.json` 里的校门、公交站用图标标出来
- 用 Canvas 画室内楼层图（走廊灰色、房间白色、楼梯橙色）
- 实现"点击建筑→进入室内视图"，"点返回→回到室外"
- 接收 B 给的路径节点序列，在地图上画线

**你的代码在**：`js/map/outdoor.js`、`js/map/indoor.js`、`js/map/layer-switch.js`

**你需要了解的数据文件**：`data/buildings.json`、`data/poi.json`、`data/indoor/*.json`

---

### 🅱️ 路径规划（nav/）

**你负责"怎么找到路"**

具体要做的：
- 实现 Graph 类（节点、边、邻接查询）—— 已写好骨架在 `js/nav/graph.js`
- 实现 A* 寻路算法 —— 已写好骨架在 `js/nav/astar.js`
- 从数据文件构建室外路网图
- 从室内数据构建室内走廊图（含楼梯电梯的跨层连接）
- 把建筑入口做成"桥梁节点"，连接室外图和室内图
- 把路径结果分段（室外段 / 2F段 / 楼梯 / 3F段），方便渲染和显示

**你的代码在**：`js/nav/graph.js`、`js/nav/astar.js`、`js/nav/outdoor-graph.js`、`js/nav/indoor-graph.js`、`js/nav/path-renderer.js`

**核心数据结构**（必须理解）：
```js
// 所有地方都用这个节点格式，室外室内统一
{
  id: "yifu-2f-corridor-north",  // 唯一ID
  type: "corridor",              // corridor|room|stair|elevator|entrance|road
  lat: 32.119, lng: 118.958,     // 坐标
  floor: 2,                      // 室外=0
  building: "yifu",              // 室外=null
  label: "逸夫楼 2F 北走廊",
  connections: ["yifu-2f-stair-west"]  // 邻接节点
}
```

---

### 🅲️ UI 与交互（ui/ + css/）

**你负责"用户怎么操作"**

具体要做的：
- 页面整体布局：顶栏搜索 + 左侧信息面板 + 中央地图
- 搜索框：输入→下拉建议（模糊匹配建筑名/房间号/POI）
- 起终点选择：搜索选 / 点地图选 / 交换按钮
- 路线信息面板：显示距离、预计时间、分段说明
- 楼层切换器：室内视图下的 1F 2F 3F... 按钮
- 移动端响应式适配
- 整体配色和视觉风格

**你的代码在**：`js/ui/search.js`、`js/ui/panel.js`、`js/ui/marker-popup.js`、`css/main.css`、`css/map.css`、`css/indoor.css`

**你的接口**（给 app.js 调的）：
- `searchBox.onChange(callback)` → 用户选了地点就通知
- `infoPanel.showRoute(routeData)` → 展示路线
- `infoPanel.showBuilding(building)` → 展示建筑信息

---

### 🅳️ 数据与文档（data/ + docs/）

**你负责"数据从哪来 + 项目怎么管"**，你是队长角色

具体要做的：
- 搜集/整理 NJU 仙林校区建筑坐标、路网、POI
- 为至少 1-2 栋建筑准备室内数据（走廊布局、楼梯电梯位置、房间编号）
- 编写所有 JSON 数据文件，确保格式符合规范（见 `docs/data-format.md`）
- 当其他三人觉得缺数据时，你负责补充
- 写技术文档 `docs/architecture.md`
- 做初期检查用的 PPT
- 管理 GitHub 仓库、Issues、看板
- 每周汇总进度，确保大家在轨道上

**你的代码在**：`data/buildings.json`、`data/outdoor-nodes.json`、`data/poi.json`、`data/indoor/*.json`、`docs/*.md`

---

## 五、怎么开始

### 第一步：拿到代码

```bash
git clone https://github.com/xianqianqianzz/-EL_project.git
cd -EL_project
```

### 第二步：创建你的分支

```bash
# A 执行这个
git checkout -b map-render
git push -u origin map-render

# B 执行这个
git checkout -b path-planning
git push -u origin path-planning

# C 执行这个
git checkout -b ui-interaction
git push -u origin ui-interaction

# D 执行这个
git checkout -b data-docs
git push -u origin data-docs
```

### 第三步：打开你的文件，开始写

代码骨架已经搭好，每个文件里都有函数声明和注释，你只需要填充实现。**在自己的分支上开发，不要直接在 master/main 上改。**

### 第四步：每天收工时 push

```bash
git add 你改的文件
git commit -m "feat(模块): 做了什么"
git push origin 你的分支名
```

详细操作见 `docs/github-guide.md`。

---

## 六、遇到问题怎么办

- **不清楚数据格式** → 先看 `docs/data-format.md`，还不行问 D
- **不知道别的模块接口** → 看 `js/app.js` 里的调用方式，或直接问那个人
- **Git 操作不会** → 看 `docs/github-guide.md`
- **需要新数据** → 找 D 补充
- **和别人代码冲突了** → 在群里说一声，一起解决

---

## 七、时间线

| 时间 | 目标 |
|------|------|
| **第1-3天** | 各模块骨架能跑通，确认和别人的接口没问题 |
| **第4-10天** | 疯狂开发各自的功能 |
| **第11-12天** | 联调——把四个模块拼起来，修 bug |
| **第13-14天** | 写文档 + 做 PPT + 准备演示 |

半个月后我们要能演示：从南门搜到逸夫楼 3 楼某教室，地图上能看到路线，点进逸夫楼能看到室内路线。

---

加油 💪
